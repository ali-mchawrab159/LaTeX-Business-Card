
name: CI
on:
  push:
    tags:
      - '*'

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Action for LaTeX

        uses: xu-cheng/latex-action@4.0.0
